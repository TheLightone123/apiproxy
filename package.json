{
  "name": "roblox-api-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Roblox API requests",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```

---

## Complete Render.com Setup

Since you're using Render, here's the full setup:

### 1. File Structure

Make sure you have these files:
```
your-project/
├── server.js          (or index.js - pick one!)
├── package.json
└── .gitignore
